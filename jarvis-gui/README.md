# JARVIS GUI - Desktop Application

Красивое десктоп-приложение для голосового ассистента JARVIS с поддержкой системного трея и горячих клавиш.

## ✨ Возможности

- 🎤 **Голосовой ввод** - запись с микрофона с автоматической детекцией тишины
- 🎨 **Современный UI** - киберпанк/футуристичный дизайн с анимациями
- 🔔 **Системный трей** - работа в фоне с индикатором статуса
- ⌨️ **Горячие клавиши** - Ctrl+Shift+Space для быстрой активации
- 📊 **Визуализация** - real-time отображение распознавания и выполнения команд
- 📜 **История** - сохранение последних 50 команд
- ⚙️ **Настройки** - конфигурация API ключей и endpoints

## 🚀 Запуск

### Предварительные требования

1. **Node.js** (14 или новее)
2. **C# Core сервер** должен быть запущен:
   ```bash
   cd ../core
   dotnet run
   ```
3. **Python dependencies**:
   ```bash
   cd ../ai-python
   pip install -r requirements.txt
   ```

### Установка и запуск

```bash
# Установить зависимости (уже выполнено)
npm install

# Запустить приложение
npm start

# Режим разработки (с DevTools)
npm run dev
```

## 🎮 Использование

### Основные действия

1. **Запуск записи:**
   - Клик на кнопку микрофона
   - Или нажмите `Ctrl+Shift+Space`

2. **Системный трей:**
   - Клик по иконке - показать/скрыть окно
   - Правый клик - меню с опциями

3. **Настройки:**
   - Нажмите кнопку "⚙️ Настройки"
   - Укажите OpenAI API Key
   - Укажите C# Core endpoint (по умолчанию: http://localhost:5055)

### Горячие клавиши

- `Ctrl+Shift+Space` - Показать окно и начать/остановить запись

## 🎨 UI Компоненты

### Главное окно
- **Индикатор статуса** - показывает текущее состояние (Готов/Слушаю/Обработка)
- **Кнопка микрофона** - с пульсирующей анимацией при записи
- **Секция транскрипта** - отображает распознанный текст
- **Секция команды** - показывает сгенерированный JSON
- **Секция результата** - ответ от C# сервера
- **История команд** - последние 50 команд с временными метками

### Системный трей
- Иконка меняется в зависимости от статуса
- Меню:
  - Show JARVIS
  - Start/Stop Listening
  - Settings
  - Quit

## 📁 Структура файлов

```
jarvis-gui/
├── main.js           # Electron main process
├── renderer.js       # UI logic and Python integration
├── index.html        # UI structure
├── styles.css        # Cyberpunk-themed styles
├── package.json      # Dependencies and scripts
├── assets/           # Icons and images
│   ├── icon.png          (нужно добавить)
│   ├── tray-icon.png     (нужно добавить)
│   ├── tray-icon-listening.png
│   └── tray-icon-idle.png
└── README.md
```

## 🔧 Настройка

### Environment Variables

Вы можете настроить приложение через переменные окружения:

- `JARVIS_CORE_ENDPOINT` - URL C# сервера (default: http://localhost:5055)
- `OPENAI_API_KEY` - Ваш OpenAI API ключ

### Конфигурационный файл

Настройки сохраняются в приложении и включают:
- C# Core endpoint
- OpenAI API Key
- Язык распознавания (ru-RU/en-US)
- Горячая клавиша

## 🎯 Следующие шаги

### TODO: Иконки
Необходимо создать иконки для приложения:
- `assets/icon.png` - основная иконка приложения (256x256)
- `assets/tray-icon.png` - иконка для трея (32x32)
- `assets/tray-icon-listening.png` - иконка "слушаю" (32x32, красная)
- `assets/tray-icon-idle.png` - иконка "готов" (32x32, зеленая)

### Возможные улучшения
- [ ] Добавить настраиваемые горячие клавиши
- [ ] Экспорт истории команд
- [ ] Темная/светлая тема
- [ ] Поддержка нескольких языков UI
- [ ] Визуализация аудио волны в реальном времени
- [ ] Уведомления о выполнении команд
- [ ] Автозапуск при старте Windows

## 📦 Сборка приложения

Для создания установщика используйте:

```bash
npm install --save-dev electron-builder
npm run build
```

Это создаст установщик для Windows в папке `dist/`.

## 🐛 Отладка

Для отладки откройте Developer Tools:
- В режиме разработки: `npm run dev` (DevTools открывается автоматически)
- Или нажмите `Ctrl+Shift+I` в приложении

## 📝 Логи

Логи Python процесса выводятся в консоль Electron (DevTools).

## 🤝 Интеграция

GUI приложение интегрируется со всем стеком JARVIS:

```
GUI (Electron) → Python (main.py) → C# Core (HTTP API) → Windows
```

1. GUI запускает Python скрипт при нажатии на микрофон
2. Python записывает аудио и отправляет в Whisper
3. Whisper транскрибирует речь в текст
4. ChatGPT генерирует JSON команду
5. Python отправляет команду в C# через HTTP
6. C# выполняет команду в Windows
7. Результат возвращается в GUI

## 📄 Лицензия

MIT
