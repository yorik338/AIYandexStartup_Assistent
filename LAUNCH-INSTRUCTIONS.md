# 🚀 Как запустить AYVOR Assistant

## ⚡ Быстрый запуск (рекомендуется)

**Просто дважды кликните на файл:**
```
START-AYVOR.bat
```

Это автоматически запустит и бэкенд, и GUI в отдельных окнах.

---

## 📋 Требования

Перед первым запуском убедитесь что установлено:

- ✅ **Node.js v22+** (для Electron GUI)
- ✅ **.NET 8.0 SDK** (для C# Core)
- ✅ **npm** (идёт вместе с Node.js)

---

## 🔧 Ручной запуск (для разработки)

### Шаг 1: Запустить C# Core (Backend)

Откройте терминал и выполните:

```bash
cd core
dotnet run
```

**Проверка:** Откройте http://localhost:5055/system/status - должен вернуться JSON с информацией о системе.

### Шаг 2: Запустить Electron GUI (Frontend)

В **НОВОМ** терминале:

**Для Windows CMD:**
```cmd
cd jarvis-gui
set ELECTRON_RUN_AS_NODE=
npm start
```

**Для Git Bash / PowerShell:**
```bash
cd jarvis-gui
unset ELECTRON_RUN_AS_NODE  # или: $env:ELECTRON_RUN_AS_NODE=""
npm start
```

---

## ⚠️ Важно: Проблема с ELECTRON_RUN_AS_NODE

Если при запуске видите ошибку:
```
TypeError: Cannot read properties of undefined (reading 'whenReady')
```

**Решение:** Удалите переменную окружения `ELECTRON_RUN_AS_NODE`

**Windows CMD:**
```cmd
set ELECTRON_RUN_AS_NODE=
```

**Git Bash:**
```bash
unset ELECTRON_RUN_AS_NODE
```

**PowerShell:**
```powershell
$env:ELECTRON_RUN_AS_NODE=""
```

**Удалить глобально (постоянно):**
1. Нажмите `Win + R`, введите `sysdm.cpl`, нажмите Enter
2. Вкладка "Дополнительно" → "Переменные среды"
3. Найдите `ELECTRON_RUN_AS_NODE` и удалите

---

## 📁 Альтернативные скрипты запуска

### Только C# Core:
```
start-core.bat
```

### Только Electron GUI:
```
start-gui.bat
```

---

## 🐛 Troubleshooting

### GUI не запускается

1. Проверьте что нет `ELECTRON_RUN_AS_NODE`:
   ```cmd
   echo %ELECTRON_RUN_AS_NODE%
   ```
   Должно быть пусто или "ECHO is off"

2. Переустановите зависимости:
   ```bash
   cd jarvis-gui
   rm -rf node_modules
   npm install
   ```

### Core не запускается (порт 5055 занят)

Проверьте что порт свободен:
```cmd
netstat -ano | findstr :5055
```

Если занят, завершите процесс:
```cmd
taskkill /PID <номер_процесса> /F
```

### Ошибка "dotnet command not found"

Установите .NET 8.0 SDK: https://dotnet.microsoft.com/download

---

## 📧 Поделиться с командой

Просто отправьте им:
1. Эту папку целиком (весь проект)
2. Файл `START-AYVOR.bat` для быстрого запуска
3. Этот README с инструкциями

**Важно:** Убедитесь что у них установлены Node.js и .NET 8.0!

---

## ✨ Новые изменения

- ✅ **Discord fix** - исправлена ошибка запуска Discord
- ✅ **Новый UI** - текстовый ввод теперь на первом месте
- ✅ **Wake Word** перемещён на вкладку "Голос"
- ✅ **Electron 33** - обновлён до последней версии

---

Создано с помощью Claude Code 🤖
